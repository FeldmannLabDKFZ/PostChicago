% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/analysis_wrappers.R
\name{makeIntsTable}
\alias{makeIntsTable}
\title{Create interactions table}
\usage{
makeIntsTable(
  L,
  baited_genes,
  repscores = FALSE,
  LL = NULL,
  ngroups = 1,
  scorecut = 5,
  readcut = 1,
  mode = "score",
  intervals = NULL,
  outfolder = NULL,
  overwrite = FALSE,
  saveFile = TRUE
)
}
\arguments{
\item{L}{list of chicagoData objects containing summarized interactions}

\item{baited_genes}{GenomicRanges object, with at least two metadata (or Values) columns: 're_id'
(fragment ID covering the bait, corresponding to fragment numbers indicated in rmapgr), genenames' (names of the baited genes).
Can be created automatically from the baitmap table (see Chicago) using the function baitmap2baited_genes().}

\item{repscores}{logical input, should replicate scores be added? if TRUE, LL must be supplied,
Default: FALSE}

\item{LL}{optional: list of chicagoData objects containing individual replicates of summarized interactions.
Names are expected to follow the naming convention 'rep1', 'rep2',..., 'repn'}

\item{ngroups}{Default: 1; number of groups in which unique view points (baits) should be processed.
Efficient computing is achieved for a maximum of ca. 100-200 entries per group.}

\item{scorecut}{Chicago score cutoff, Default: 5 (as recommended by Chicago)}

\item{readcut}{reads cutoff, optional: Default: 1}

\item{mode}{used for cutoff when getting interactions, by 'score', 'read' or 'both', Default: 'score'}

\item{intervals}{optional: A GenomicRanges object with intervals with which the otherEnds should overlap.
Useful if mode='reads', to reduce the number of interactions in ints}

\item{outfolder}{optional: Path to the directory in which the output is saved, Default: current folder}

\item{overwrite}{If TRUE runs the function even if an ints_all.txt is present in the outputdir, otherwise loads ints.txt
file if present. Default: FALSE}

\item{saveFile}{If TRUE the file is saved. Default: TRUE}
}
\value{
table of all interactions, which can be saved as .txt and saves a QC plot
via correlation heatmap between samples
}
\description{
makeIntsTable() integrates significant interactions from multiple data sets supplied in the list L.
The table is populated by scores, raw and downsampled reads and optionally with replicate data.
}
\examples{
# example code

##Create an rmapgr object
rmapgr <- GenomicRanges::GRanges(rep(1,5), IRanges::IRanges( 1:5,2:6))
rmapgr$id=1:5

##create baited_genes with one gene
baited_genes <- GenomicRanges::GRanges(1,IRanges::IRanges(1,2)) 
baited_genes$re_id=1
baited_genes$genename='testgene'

##create a list of chicagodata objects with two samples
cdlist <- list(
sample1 = data.frame(baitID=rep(1,4),
                      otherEndID=c(2,3:5),
                      N=c(2,5,10,8),
                      N.1=c(2,5,10,8),
                      N.2=c(3,4,9,7),
                      score=c(5, 1, 2, 4)),
 
sample2 = data.frame(baitID=rep(1,4),
                      otherEndID=c(2,3:5),
                      N=c(20,3,1,6),
                      N.1=c(20,3,1,6),
                      N.2=c(17,4,2,3),
                      score=c(3, 5, 3, 5))
      )

##run makeIntsTable
ints = makeIntsTable(cdlist,baited_genes,saveFile=FALSE)
head(ints)

}
