% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/plotting_v11.R
\name{plotAggregatePeaks}
\alias{plotAggregatePeaks}
\title{Plot Aggregate Interaction Profiles}
\usage{
plotAggregatePeaks(
  Lm,
  mainprefix = NULL,
  col = NULL,
  ylim = NULL,
  lty = NULL,
  xlim = NULL,
  k = 3,
  ylab = "normalised reads",
  plotwhat = "all"
)
}
\arguments{
\item{Lm}{List of matrices containing interaction reads or scores.}

\item{mainprefix}{Optional; text added to the plot title.}

\item{col}{Custom colors for aggregate plots. Default: 4 colors.}

\item{ylim}{Optional}

\item{lty}{Optional; Line type for the aggregate plots}

\item{xlim}{Optional; should be added in restriction fragments surrounding the interval/peak center. Example: c(-50,50).
Default: All restriction fragments represented in the matrix.}

\item{k}{Numeric; over how many fragments should the running mean be calculated? Default: 3}

\item{ylab}{Optional; Default: 'normalised reads'}

\item{plotwhat}{Defines what types of plots will be plotted. Options are 'all','mean','median'
'mean' and 'median' plot running averages over k fragments.
Default: 'all': Four plots representing mean and median read counts both smoothed and unsmoothed over k fragments.}
}
\value{
Plot containing aggregate profiles of interactions.
}
\description{
Takes a list of matrices and plots aggregate interaction signal, similar for metaprofiles in ChIP-Seq.
}
\examples{
# example code

##create list of matrices:

m1<-matrix(c(c(1:10,11,9:0)*sample((8:1.2)/10,21,replace=TRUE),
            c(1:10,11,9:0)*sample((8:1.2)/10,21,replace=TRUE),
            c(1:10,11,9:0)*sample((8:1.2)/10,21,replace=TRUE)),nrow=3)
m2<-matrix(c(c(21:30,39,29:20)*sample((8:1.2)/10,21,replace=TRUE),
            c(21:30,39,29:20)*sample((8:1.2)/10,21,replace=TRUE),
            c(21:30,39,29:20)*sample((8:1.2)/10,21,replace=TRUE)),nrow=3)
rownames(m1)=c('1;2','2;3','3;4')
rownames(m2)=rownames(m1)

Lm=list(m1=m1,m2=m2,m1_con=m1*0.2,m2_con=m2*0.1)

##plot median profiles::
plotAggregatePeaks(Lm,plotwhat='median',k=3,ylab='reads',
                  col=rep(c('red','blue'),2), lty=c(1,1,3,3))

##plot mean profiles::
plotAggregatePeaks(Lm,plotwhat='mean',k=3,ylab='reads',
                  col=rep(c('red','blue'),2), lty=c(1,1,3,3))

}
