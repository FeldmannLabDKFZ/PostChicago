% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/PostChicagoTidyNestedRemoved.R
\name{plotAggregatePeaks}
\alias{plotAggregatePeaks}
\title{Plot Aggregate Interaction Profiles}
\usage{
plotAggregatePeaks(
  Lm,
  mainprefix = NULL,
  col = NULL,
  ylim = NULL,
  lty = NULL,
  xlim = NULL,
  k = 3,
  ylab = "normalised reads",
  plotwhat = "all"
)
}
\arguments{
\item{Lm}{List of matrices containing interaction reads or scores}

\item{mainprefix}{Optional; text added to the plot title.}

\item{col}{Optional; colour for the aggregate plots. Default contains four colours.}

\item{ylim}{Optional; Default data-driven}

\item{lty}{Optional; line type for the aggregate plots}

\item{xlim}{Optional; should be added in restriction fragments surrounding the interval/peak center. Example: c(-50,50).
Default: All restriction fragments represented in the matrix.}

\item{k}{Numeric; over how many fargments should the running mean be calculated? Default: 3}

\item{ylab}{Optional. Default: 'normalised reads'}

\item{plotwhat}{What plots to plot? Options are 'all','mean','median'
'mean' and 'median' plot running averages over k fragments.
Default: 'all': Four plots representing mean and median read counts both smoothed and unsmoothed.}
}
\value{
Plot containing aggregate matric profiles
}
\description{
Takes a list of matrices and plots aggregate profiles
}
\examples{
# example code

#requires precomputed matrices in the resfolder (see getMatrix())
extdata <- system.file("extdata", package="PostChicago")
outputDir <- file.path(extdata,'postchicago')
resfolder=paste0(outputDir,'/matrices/')

#load list of matrices supplied in the PostChicago package (Lm):
load(paste0(resfolder,'matrices_K27ac_peaks_reads'))

#plot aggregate profiles:
plotAggregatePeaks(Lm,plotwhat='median',k=3,xlim=c(-50,50),mainprefix= 'K27ac\n', ylab='reads', 
                    col=rep(c('red','blue'),2), lty=c(1,1,3,3))

}
